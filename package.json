{
  "name": "drivex-platform",
  "version": "0.1.0",
  "private": true,
  "description": "DriveX platform monorepo",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  },
  "packageManager": "npm@11.13.0",
  "engines": {
    "node": ">=18.17.0"
  }
}
