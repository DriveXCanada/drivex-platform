// Shared constants for the expenses feature (safe to import anywhere — this is
// NOT a "use server" module, so it can export non-function values).

/** settings.key under which the recurring monthly expense budget is stored. */
export const BUDGET_KEY = "monthly_expense_budget";
